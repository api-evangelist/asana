naftiko: 1.0.0-alpha2
info:
  label: Asana — Project Memberships
  description: 'Asana — Project Memberships. 2 operations. Lead operation: Asana Get a project membership. Self-contained
    Naftiko capability covering one Asana business surface.'
  tags:
  - Asana
  - Project Memberships
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASANA_API_KEY: ASANA_API_KEY
capability:
  consumes:
  - type: http
    namespace: asana-project-memberships
    baseUri: https://app.asana.com/api/1.0
    description: Asana — Project Memberships business capability. Self-contained, no shared references.
    resources:
    - name: project_memberships-project_membership_gid
      path: /project_memberships/{project_membership_gid}
      operations:
      - name: getprojectmembership
        method: GET
        description: Asana Get a project membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    - name: projects-project_gid-project_memberships
      path: /projects/{project_gid}/project_memberships
      operations:
      - name: getprojectmembershipsforproject
        method: GET
        description: Asana Get memberships from a project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    authentication:
      type: bearer
      token: '{{env.ASANA_API_KEY}}'
  exposes:
  - type: rest
    namespace: asana-project-memberships-rest
    port: 8080
    description: REST adapter for Asana — Project Memberships. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/project-memberships/{project-membership-gid}
      name: project-memberships-project-membership-gid
      description: REST surface for project_memberships-project_membership_gid.
      operations:
      - method: GET
        name: getprojectmembership
        description: Asana Get a project membership
        call: asana-project-memberships.getprojectmembership
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/projects/{project-gid}/project-memberships
      name: projects-project-gid-project-memberships
      description: REST surface for projects-project_gid-project_memberships.
      operations:
      - method: GET
        name: getprojectmembershipsforproject
        description: Asana Get memberships from a project
        call: asana-project-memberships.getprojectmembershipsforproject
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: asana-project-memberships-mcp
    port: 9090
    transport: http
    description: MCP adapter for Asana — Project Memberships. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: asana-get-project-membership
      description: Asana Get a project membership
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-project-memberships.getprojectmembership
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-get-memberships-project
      description: Asana Get memberships from a project
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-project-memberships.getprojectmembershipsforproject
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
