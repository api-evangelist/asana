naftiko: 1.0.0-alpha2
info:
  label: Asana — Workspace Memberships
  description: 'Asana — Workspace Memberships. 3 operations. Lead operation: Asana Get workspace memberships for a user. Self-contained
    Naftiko capability covering one Asana business surface.'
  tags:
  - Asana
  - Workspace Memberships
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASANA_API_KEY: ASANA_API_KEY
capability:
  consumes:
  - type: http
    namespace: asana-workspace-memberships
    baseUri: https://app.asana.com/api/1.0
    description: Asana — Workspace Memberships business capability. Self-contained, no shared references.
    resources:
    - name: users-user_gid-workspace_memberships
      path: /users/{user_gid}/workspace_memberships
      operations:
      - name: getworkspacemembershipsforuser
        method: GET
        description: Asana Get workspace memberships for a user
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    - name: workspace_memberships-workspace_membership_gid
      path: /workspace_memberships/{workspace_membership_gid}
      operations:
      - name: getworkspacemembership
        method: GET
        description: Asana Get a workspace membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    - name: workspaces-workspace_gid-workspace_memberships
      path: /workspaces/{workspace_gid}/workspace_memberships
      operations:
      - name: getworkspacemembershipsforworkspace
        method: GET
        description: Asana Get the workspace memberships for a workspace
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    authentication:
      type: bearer
      token: '{{env.ASANA_API_KEY}}'
  exposes:
  - type: rest
    namespace: asana-workspace-memberships-rest
    port: 8080
    description: REST adapter for Asana — Workspace Memberships. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users/{user-gid}/workspace-memberships
      name: users-user-gid-workspace-memberships
      description: REST surface for users-user_gid-workspace_memberships.
      operations:
      - method: GET
        name: getworkspacemembershipsforuser
        description: Asana Get workspace memberships for a user
        call: asana-workspace-memberships.getworkspacemembershipsforuser
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workspace-memberships/{workspace-membership-gid}
      name: workspace-memberships-workspace-membership-gid
      description: REST surface for workspace_memberships-workspace_membership_gid.
      operations:
      - method: GET
        name: getworkspacemembership
        description: Asana Get a workspace membership
        call: asana-workspace-memberships.getworkspacemembership
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workspaces/{workspace-gid}/workspace-memberships
      name: workspaces-workspace-gid-workspace-memberships
      description: REST surface for workspaces-workspace_gid-workspace_memberships.
      operations:
      - method: GET
        name: getworkspacemembershipsforworkspace
        description: Asana Get the workspace memberships for a workspace
        call: asana-workspace-memberships.getworkspacemembershipsforworkspace
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: asana-workspace-memberships-mcp
    port: 9090
    transport: http
    description: MCP adapter for Asana — Workspace Memberships. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: asana-get-workspace-memberships-user
      description: Asana Get workspace memberships for a user
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-workspace-memberships.getworkspacemembershipsforuser
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-get-workspace-membership
      description: Asana Get a workspace membership
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-workspace-memberships.getworkspacemembership
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-get-workspace-memberships-workspace
      description: Asana Get the workspace memberships for a workspace
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-workspace-memberships.getworkspacemembershipsforworkspace
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
