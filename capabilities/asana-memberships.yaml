naftiko: 1.0.0-alpha2
info:
  label: Asana — Memberships
  description: 'Asana — Memberships. 5 operations. Lead operation: Asana Get multiple memberships. Self-contained Naftiko
    capability covering one Asana business surface.'
  tags:
  - Asana
  - Memberships
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASANA_API_KEY: ASANA_API_KEY
capability:
  consumes:
  - type: http
    namespace: asana-memberships
    baseUri: https://app.asana.com/api/1.0
    description: Asana — Memberships business capability. Self-contained, no shared references.
    resources:
    - name: memberships
      path: /memberships
      operations:
      - name: getmemberships
        method: GET
        description: Asana Get multiple memberships
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: parent
          in: query
          type: string
          description: Globally unique identifier for `goal`, `project`, or `portfolio`.
        - name: member
          in: query
          type: string
          description: Globally unique identifier for `team` or `user`.
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
      - name: createmembership
        method: POST
        description: Asana Create a membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    - name: memberships-membership_gid
      path: /memberships/{membership_gid}
      operations:
      - name: getmembership
        method: GET
        description: Asana Get a membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
      - name: updatemembership
        method: PUT
        description: Asana Update a membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletemembership
        method: DELETE
        description: Asana Delete a membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ASANA_API_KEY}}'
  exposes:
  - type: rest
    namespace: asana-memberships-rest
    port: 8080
    description: REST adapter for Asana — Memberships. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/memberships
      name: memberships
      description: REST surface for memberships.
      operations:
      - method: GET
        name: getmemberships
        description: Asana Get multiple memberships
        call: asana-memberships.getmemberships
        with:
          parent: rest.parent
          member: rest.member
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createmembership
        description: Asana Create a membership
        call: asana-memberships.createmembership
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/memberships/{membership-gid}
      name: memberships-membership-gid
      description: REST surface for memberships-membership_gid.
      operations:
      - method: GET
        name: getmembership
        description: Asana Get a membership
        call: asana-memberships.getmembership
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: updatemembership
        description: Asana Update a membership
        call: asana-memberships.updatemembership
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletemembership
        description: Asana Delete a membership
        call: asana-memberships.deletemembership
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: asana-memberships-mcp
    port: 9090
    transport: http
    description: MCP adapter for Asana — Memberships. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: asana-get-multiple-memberships
      description: Asana Get multiple memberships
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-memberships.getmemberships
      with:
        parent: tools.parent
        member: tools.member
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-create-membership
      description: Asana Create a membership
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: asana-memberships.createmembership
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-get-membership
      description: Asana Get a membership
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-memberships.getmembership
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-update-membership
      description: Asana Update a membership
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: asana-memberships.updatemembership
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-delete-membership
      description: Asana Delete a membership
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: asana-memberships.deletemembership
      outputParameters:
      - type: object
        mapping: $.
