naftiko: 1.0.0-alpha2
info:
  label: Asana — Portfolio Memberships
  description: 'Asana — Portfolio Memberships. 3 operations. Lead operation: Asana Get multiple portfolio memberships. Self-contained
    Naftiko capability covering one Asana business surface.'
  tags:
  - Asana
  - Portfolio Memberships
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ASANA_API_KEY: ASANA_API_KEY
capability:
  consumes:
  - type: http
    namespace: asana-portfolio-memberships
    baseUri: https://app.asana.com/api/1.0
    description: Asana — Portfolio Memberships business capability. Self-contained, no shared references.
    resources:
    - name: portfolio_memberships
      path: /portfolio_memberships
      operations:
      - name: getportfoliomemberships
        method: GET
        description: Asana Get multiple portfolio memberships
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    - name: portfolio_memberships-portfolio_membership_gid
      path: /portfolio_memberships/{portfolio_membership_gid}
      operations:
      - name: getportfoliomembership
        method: GET
        description: Asana Get a portfolio membership
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    - name: portfolios-portfolio_gid-portfolio_memberships
      path: /portfolios/{portfolio_gid}/portfolio_memberships
      operations:
      - name: getportfoliomembershipsforportfolio
        method: GET
        description: Asana Get memberships from a portfolio
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: opt_fields
          in: query
          type: array
          description: This endpoint returns a compact resource, which excludes some properties by default. To include those
            optional properties, set this query parameter to a comma-s
    authentication:
      type: bearer
      token: '{{env.ASANA_API_KEY}}'
  exposes:
  - type: rest
    namespace: asana-portfolio-memberships-rest
    port: 8080
    description: REST adapter for Asana — Portfolio Memberships. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/portfolio-memberships
      name: portfolio-memberships
      description: REST surface for portfolio_memberships.
      operations:
      - method: GET
        name: getportfoliomemberships
        description: Asana Get multiple portfolio memberships
        call: asana-portfolio-memberships.getportfoliomemberships
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/portfolio-memberships/{portfolio-membership-gid}
      name: portfolio-memberships-portfolio-membership-gid
      description: REST surface for portfolio_memberships-portfolio_membership_gid.
      operations:
      - method: GET
        name: getportfoliomembership
        description: Asana Get a portfolio membership
        call: asana-portfolio-memberships.getportfoliomembership
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/portfolios/{portfolio-gid}/portfolio-memberships
      name: portfolios-portfolio-gid-portfolio-memberships
      description: REST surface for portfolios-portfolio_gid-portfolio_memberships.
      operations:
      - method: GET
        name: getportfoliomembershipsforportfolio
        description: Asana Get memberships from a portfolio
        call: asana-portfolio-memberships.getportfoliomembershipsforportfolio
        with:
          opt_fields: rest.opt_fields
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: asana-portfolio-memberships-mcp
    port: 9090
    transport: http
    description: MCP adapter for Asana — Portfolio Memberships. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: asana-get-multiple-portfolio-memberships
      description: Asana Get multiple portfolio memberships
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-portfolio-memberships.getportfoliomemberships
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-get-portfolio-membership
      description: Asana Get a portfolio membership
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-portfolio-memberships.getportfoliomembership
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
    - name: asana-get-memberships-portfolio
      description: Asana Get memberships from a portfolio
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: asana-portfolio-memberships.getportfoliomembershipsforportfolio
      with:
        opt_fields: tools.opt_fields
      outputParameters:
      - type: object
        mapping: $.
